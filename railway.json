{
  "name": "June-X Ultra",
  "description": "Professional WhatsApp Bot with auto-session authentication",
  "repository": "https://github.com/Vinpink2/June-Ultra",
  "build": {
    "builder": "DOCKERFILE",
    "buildCommand": "npm install"
  },
  "deploy": {
    "startCommand": "node index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  },
  "variables": {
    "SESSION_ID": {
      "description": "REQUIRED: Your session token (june-ultra:~...) from the pairing page",
      "required": true
    },
    "REPO_URL": {
      "description": "Optional: override the bot source zip URL the loader downloads at startup",
      "required": false
    }
  }
}
